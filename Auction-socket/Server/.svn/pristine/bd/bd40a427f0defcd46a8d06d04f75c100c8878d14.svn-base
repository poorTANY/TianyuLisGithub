import java.sql.SQLException;

public class Main {
	public static void main(String[] a) throws SQLException {
		String port;
		if (a.length == 0) {
			port = "8000";
		} else {
			port = a[0];
		}
		new Server(port);
	}
}
